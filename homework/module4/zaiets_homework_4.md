# Домашнє завдання №4. Пакети, сервіси та журнали
**Студент:** Заєць Влад

## Завдання 1. Менеджери пакетів
1. Оновлення списку пакетів у системі: `sudo apt update`
2. Встановлення утиліти tree: `sudo apt install tree -y`
3. Перевірка, чи встановлено пакет, і вивід його версії: `tree --version`
4. Видалення встановленого пакету: `sudo apt remove tree -y`

## Завдання 2. Керування сервісами через systemctl
1. Перевірка статусу сервісу ssh: `systemctl status ssh`
2. Зупинка сервісу: `sudo systemctl stop ssh`
3. Запуск сервісу знову: `sudo systemctl start ssh`
4. Додавання сервісу в автозавантаження: `sudo systemctl enable ssh`

## Завдання 3. Робота з логами
1. Перехід у директорію /var/log і вивід останніх 10 рядків syslog:
```bash
   cd /var/log
   tail -n 10 syslog